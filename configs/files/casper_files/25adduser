#!/bin/sh
set -eu

PREREQ=""
DESCRIPTION="Preparing live session..."

prereqs() {
    echo "$PREREQ"
}

case "$1" in
    prereqs)
        prereqs
        exit 0
        ;;
esac

. /scripts/casper-functions

log_begin_msg "$DESCRIPTION"

log_end_msg
