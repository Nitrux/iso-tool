#! /bin/bash

set -e

#	Source APT commands as functions.
#	shellcheck source=/dev/null

source "$PWD"/configs/scripts/others/apt-funcs

HOST_PKGS='
	axel
'

XORRISO_PKGS='
	libburn4
	libisoburn1
	libisofs6
	libjte2
	mtools
	sshpass
	xorriso
'

GRUB_EFI_PKGS='
	dkms
	mokutil
	grub-efi-amd64
	grub-efi-amd64-signed
	shim-signed
'

update
install $XORRISO_PKGS $GRUB_EFI_PKGS $HOST_PKGS
