dist: trusty

sudo: required

before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install -y squashfs-tools xorriso syslinux-common isolinux

script:
  - sudo ./build.sh

after_success:
  - sudo chmod a+rw nitruxos.iso
  - sh ./transfer.sh

branches:
  except:
    - /^(?i:continuous)$/

notifications:
  email: false
