dist: bionic

sudo: required

before_install:
  - uname -a
  - sudo apt-get -qq update
  - sudo apt-get -qq install -y squashfs-tools xorriso syslinux-common isolinux debootstrap tree

script:
  - sudo ./build.sh

after_success:
  - sudo chmod a+rw nitruxos.iso
  - sh ./upload.sh

branches:
  except:
    - /^(?i:continuous)$/

notifications:
  email: false
